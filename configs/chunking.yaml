markdown:
  max_chars: 1800
  overlap_chars: 200

text:
  max_chars: 2200
  overlap_chars: 250

code:
  max_chars: 2600
  overlap_chars: 300

php:
  strategy: "tree-sitter"
  max_chars: 2600
  overlap_chars: 300
  max_uses_in_prefix: 24
  fallback_strategy: "line"
